diff --git a/dist/runtime.js b/dist/runtime.js
index 3fb25ae..983be86 100644
--- a/dist/runtime.js
+++ b/dist/runtime.js
@@ -734,17 +734,32 @@ async function createOrLoadRuntimeSession(client, resumeSessionId, cwd) {
 		sessionResult: created
 	};
 }
+function assertNativeContextRecord(record, identity) {
+		const normalized = normalizeAgentCommandInput(identity.command);
+		if (record.name !== identity.sessionKey || record.acpxRecordId !== identity.acpxRecordId || record.acpSessionId !== identity.backendSessionId
+			|| record.agentSessionId !== identity.agentSessionId || record.cwd !== identity.cwd || record.agentCommand !== normalized.agentCommand || JSON.stringify(record.agentArgv) !== JSON.stringify(identity.command)
+			|| record.acpx?.dutydeck_native_creation_id !== identity.nativeCreationId || !sameStrictIdentity(record.acpx?.dutydeck_native_identity, identity)) throw new Error("ACP_NATIVE_CONTEXT_IDENTITY_CONFLICT");
+	}
+function sameStrictIdentity(raw, expected) {
+  try {
+    const stored = JSON.parse(raw);
+    const canonical = value => Array.isArray(value) ? value.map(canonical) : value && typeof value === "object" ? Object.fromEntries(Object.keys(value).sort().map(key => [key,canonical(value[key])])) : value;
+    return JSON.stringify(canonical(stored)) === JSON.stringify(canonical(expected));
+  } catch { return false; }
+}
 var AcpRuntimeManager = class {
 	options;
 	deps;
 	activeControllers = /* @__PURE__ */ new Map();
 	pendingPersistentClients = /* @__PURE__ */ new Map();
+	strictIdentities = new Map();
 	closingActiveRecords = /* @__PURE__ */ new Set();
 	constructor(options, deps = {}) {
 		this.options = options;
 		this.deps = deps;
 	}
 	createClient(options) {
+		options = { ...options, onProcess: this.options.onProcess };
 		return this.deps.clientFactory?.(options) ?? new AcpClient(options);
 	}
 	async readPendingPersistentClient(record, options) {
@@ -822,6 +837,7 @@ var AcpRuntimeManager = class {
 		};
 	}
 	async ensureSession(input) {
+		if (input.strict) return await this.ensureStrictSession(input);
 		const cwd = path.resolve(input.cwd?.trim() || this.options.cwd);
 		const { agentCommand, agentArgv } = normalizeAgentCommandInput(this.options.agentRegistry.resolve(input.agent));
 		const existing = await this.options.sessionStore.load(input.sessionKey);
@@ -849,7 +865,7 @@ var AcpRuntimeManager = class {
 		});
 		let keepClientOpen = false;
 		try {
-			await client.start();
+			await withTimeout(client.start(), this.options.timeoutMs);
 			const session = await createOrLoadRuntimeSession(client, input.resumeSessionId, cwd);
 			const record = await this.createAndSaveRuntimeRecord({
 				input,
@@ -865,6 +881,51 @@ var AcpRuntimeManager = class {
 			if (!keepClientOpen) await client.close();
 		}
 	}
+
+	assertStrictRecord(record, identity) { assertNativeContextRecord(record,identity); }
+	async ensureStrictSession(input) {
+		const cwd = path.resolve(input.cwd?.trim() || this.options.cwd);
+		const {agentCommand, agentArgv} = normalizeAgentCommandInput(this.options.agentRegistry.resolve(input.agent));
+		const original = await this.options.sessionStore.load(input.sessionKey);
+		const expected = input.strict.expected;
+        if(expected&&expected.agent!==input.agent)throw new Error("ACP_NATIVE_CONTEXT_IDENTITY_CONFLICT");
+		if (input.strict.mode === "create") {
+			if (original) throw new Error("ACP_NATIVE_CONTEXT_ALREADY_EXISTS");
+		} else {
+			if (!original || !expected) throw new Error("ACP_NATIVE_CONTEXT_MISSING");
+			this.assertStrictRecord(original, expected);
+			if (agentCommand !== original.agentCommand || cwd !== original.cwd) throw new Error("ACP_NATIVE_CONTEXT_IDENTITY_CONFLICT");
+		}
+		const client = this.createClient({agentCommand,agentArgv,cwd,mcpServers:[...this.options.mcpServers??[]],permissionMode:this.options.permissionMode,
+			nonInteractivePermissions:this.options.nonInteractivePermissions,onPermissionRequest:this.options.onPermissionRequest,sessionOptions:input.sessionOptions,resourceScope:input.resourceScope});
+		let kept = false;
+		try {
+			await withTimeout(client.start(), this.options.timeoutMs);
+			const session = await createOrLoadRuntimeSession(client, input.strict.mode === "restore" ? expected.backendSessionId : void 0, cwd);
+			const record = original ? JSON.parse(JSON.stringify(original)) : createInitialRecord({recordId:input.sessionKey,sessionName:input.sessionKey,
+				sessionId:session.sessionId,agentCommand,agentArgv,cwd,agentSessionId:session.agentSessionId});
+			if (original && (session.sessionId !== expected.backendSessionId || session.agentSessionId !== void 0 && session.agentSessionId !== expected.agentSessionId)) throw new Error("ACP_NATIVE_CONTEXT_IDENTITY_CONFLICT");
+			applyConfigOptionsToRecord(record, session.sessionResult);
+			syncAdvertisedModelState(record, session.sessionResult.models);
+			const defaults = {};
+			for (const option of session.sessionResult.configOptions ?? []) {
+				if (option.id === "model" || option.category === "model") defaults.model = option.currentValue;
+				if (["reasoning_effort","effort"].includes(option.id) || option.category === "thought_level") defaults.reasoningEffort = option.currentValue;
+			}
+			if (!defaults.model && session.sessionResult.models?.currentModelId) defaults.model = session.sessionResult.models.currentModelId;
+			const identity = expected ?? {...input.strict.creation,acpxRecordId:record.acpxRecordId,backendSessionId:record.acpSessionId,
+				...(record.agentSessionId ? {agentSessionId:record.agentSessionId}:{}),defaults};
+			record.acpx = {...record.acpx,dutydeck_native_creation_id:identity.nativeCreationId,dutydeck_native_identity:JSON.stringify(identity)};
+			persistSessionOptions(record, original ? {...sessionOptionsFromRecord(original),env:input.sessionOptions?.env??{}} : input.sessionOptions);
+			applyLifecycleSnapshotToRecord(record,client.getAgentLifecycleSnapshot());
+			record.closed=false;delete record.closedAt;
+			await this.options.sessionStore.save(record);
+			await input.strict.onPrepared(identity, defaults);
+			this.strictIdentities.set(record.acpxRecordId,identity);
+			kept=await this.keepPersistentClient("persistent",record.acpxRecordId,client);
+			return record;
+		} finally { if (!kept) await client.close(); }
+	}
 	async createAndSaveRuntimeRecord(params) {
 		const { input, client, agentCommand, agentArgv, cwd, session } = params;
 		const record = createInitialRecord({
@@ -986,11 +1047,25 @@ var AcpRuntimeManager = class {
 				client: turn.client,
 				sessionId,
 				prompt: task.promptInput,
+				resourceScope: task.input.resourceScope, beforePrompt: task.input.beforePrompt,
 				timeoutMs: task.input.timeoutMs ?? this.options.timeoutMs,
 				conversation: turn.conversation,
 				promptMessageId: turn.promptMessageId
 			});
 			await this.saveCompletedRuntimeTurn(turn, response.stopReason);
+			// Only the current prompt response proves a terminal provider failure.
+			// Session updates may describe an older turn or a retryable warning.
+			const air = response._meta?.jetbrains?.air;
+			const failure = air?.sessionFailure;
+			if (air?.version === 1 && failure?.severity === "error"
+				&& typeof failure.id === "string" && failure.id.length > 0
+				&& Number.isInteger(failure.revision) && failure.revision > 0
+				&& typeof failure.category === "string" && typeof failure.title === "string") {
+				task.settleResult({ status: "failed", error: {
+					code: "ACP_PROVIDER_TERMINAL_ERROR", message: failure.title, detailCode: failure.category
+				} });
+				return;
+			}
 			task.settleResult({
 				status: response.stopReason === "cancelled" ? "cancelled" : "completed",
 				...response.stopReason ? { stopReason: response.stopReason } : {}
@@ -1014,7 +1089,7 @@ var AcpRuntimeManager = class {
 		applyConversation(record, conversation);
 		await this.options.sessionStore.save(record);
 		const pendingClient = await this.readPendingPersistentClient(record, { consume: true });
-		const client = pendingClient ?? this.createTurnClient(record);
+		const client = pendingClient ?? this.createTurnClient(record, task.input.resourceScope);
 		const turn = {
 			record,
 			conversation,
@@ -1030,7 +1105,7 @@ var AcpRuntimeManager = class {
 		this.installRuntimeTurnEventHandlers(task, turn);
 		return turn;
 	}
-	createTurnClient(record) {
+	createTurnClient(record, resourceScope) {
 		return this.createClient({
 			agentCommand: record.agentCommand,
 			agentArgv: record.agentArgv,
@@ -1040,7 +1115,7 @@ var AcpRuntimeManager = class {
 			nonInteractivePermissions: this.options.nonInteractivePermissions,
 			onPermissionRequest: this.options.onPermissionRequest,
 			verbose: this.options.verbose,
-			sessionOptions: sessionOptionsFromRecord(record)
+			sessionOptions: sessionOptionsFromRecord(record), resourceScope
 		});
 	}
 	createRuntimeTurnCheckpoint(record, conversation, readAcpxState) {
@@ -1271,6 +1346,22 @@ var AcpRuntimeManager = class {
 			}
 		};
 	}
+	async setStrictConfigOption(handle, key, value) {
+        const record = await this.requireRecord(handle.acpxRecordId ?? handle.sessionKey);
+        if (!this.strictIdentities.has(record.acpxRecordId)) throw new Error("ACP_NATIVE_CONTEXT_PROOF_REQUIRED");
+        if (this.activeControllers.has(record.acpxRecordId)) throw new Error("ACP_CONFIG_TURN_ACTIVE");
+        const client = await this.readPendingPersistentClient(record,{consume:false});
+        if (!client) throw new Error("ACP_NATIVE_CONTEXT_RESTORE_REQUIRED");
+        const advertised=record.acpx?.config_options?.find(option=>option.id===key||key==="reasoning_effort"&&option.category==="thought_level"||key==="model"&&option.category==="model");
+        const configId = resolveSupportedConfigOptionId(record,advertised?.id??key);
+        const response = await client.setSessionConfigOption(record.acpSessionId,configId,value);
+        if (!response.configOptions?.some(option => option.id === configId && option.currentValue === value)) throw new Error("ACP_CONFIG_ACK_UNVERIFIED");
+        await this.requireRecord(record.acpxRecordId);
+        applyConfigOptionsToRecord(record,response);
+        applyDesiredConfigOptionToRecord(record,configId,value);
+        await this.options.sessionStore.save(record);
+        return {configId,value,configOptions:response.configOptions};
+    }
 	async setMode(handle, mode, sessionMode = "persistent") {
 		const record = await this.requireRecord(handle.acpxRecordId ?? handle.sessionKey);
 		const controller = this.activeControllers.get(record.acpxRecordId);
@@ -1303,7 +1394,9 @@ var AcpRuntimeManager = class {
 		await this.activeControllers.get(handle.acpxRecordId ?? handle.sessionKey)?.requestCancelActivePrompt();
 	}
 	async close(handle, options = {}) {
-		const record = await this.requireRecord(handle.acpxRecordId ?? handle.sessionKey);
+        let record;
+        try { record = await this.requireRecord(handle.acpxRecordId ?? handle.sessionKey); }
+        catch (error) { await this.cancel(handle); await this.closePendingPersistentClient(handle.acpxRecordId ?? handle.sessionKey); throw error; }
 		if (this.activeControllers.has(record.acpxRecordId)) this.closingActiveRecords.add(record.acpxRecordId);
 		await this.cancel(handle);
 		if (options.discardPersistentState) {
@@ -1344,6 +1437,8 @@ var AcpRuntimeManager = class {
 	async requireRecord(sessionId) {
 		const record = await this.options.sessionStore.load(sessionId);
 		if (!record) throw new Error(`ACP session not found: ${sessionId}`);
+		const expected = this.strictIdentities.get(sessionId);
+		if (expected) this.assertStrictRecord(record,expected);
 		return record;
 	}
 };
@@ -1571,6 +1666,8 @@ var AcpxRuntime = class {
 			details: normalizeRuntimeDetails(report.details)
 		};
 	}
+	async createStrictSession(input) { return await this.ensureSession({...input,mode:"persistent",strict:{mode:"create",creation:input.creation,onPrepared:input.onPrepared}}); }
+	async restoreStrictSession(input) { return await this.ensureSession({...input,mode:"persistent",strict:{mode:"restore",expected:input.expected,onPrepared:input.onPrepared}}); }
 	async ensureSession(input) {
 		const sessionName = input.sessionKey.trim();
 		if (!sessionName) throw new AcpRuntimeError("ACP_SESSION_INIT_FAILED", "ACP session key is required.");
@@ -1582,7 +1679,7 @@ var AcpxRuntime = class {
 			mode: input.mode,
 			cwd: input.cwd ?? this.options.cwd,
 			resumeSessionId: input.resumeSessionId,
-			sessionOptions: input.sessionOptions
+			sessionOptions: input.sessionOptions, strict: input.strict, resourceScope: input.resourceScope
 		});
 		const handle = {
 			sessionKey: input.sessionKey,
@@ -1614,7 +1711,7 @@ var AcpxRuntime = class {
 			sessionMode: state.mode,
 			requestId: input.requestId,
 			timeoutMs: input.timeoutMs,
-			signal: input.signal
+			signal: input.signal, resourceScope: input.resourceScope, beforePrompt: input.beforePrompt
 		}));
 		return {
 			requestId: input.requestId,
@@ -1660,6 +1757,10 @@ var AcpxRuntime = class {
 		const { handle } = this.resolveManagerHandle(input.handle);
 		return await (await this.getManager()).getStatus(handle);
 	}
+	async setStrictConfigOption(input) {
+        const manager = await this.getManager();
+        return await manager.setStrictConfigOption(this.resolveManagerHandle(input.handle).handle,input.key,input.value);
+    }
 	async setMode(input) {
 		const { handle, state } = this.resolveManagerHandle(input.handle);
 		await (await this.getManager()).setMode(handle, input.mode, state.mode);
@@ -1725,6 +1826,6 @@ function createRuntimeStore(options) {
 	return createFileSessionStore(options);
 }
 //#endregion
-export { ACPX_BACKEND_ID, AcpRuntimeError, AcpxRuntime, DEFAULT_AGENT_NAME, REQUESTED_MODEL_UNSUPPORTED_ERROR_CODE, REQUESTED_MODEL_UNSUPPORTED_REASONS, RequestedModelUnsupportedError, createAcpRuntime, createAgentRegistry, createFileSessionStore, createRuntimeStore, decodeAcpxRuntimeHandleState, encodeAcpxRuntimeHandleState, isAcpRuntimeError, isRequestedModelUnsupportedError };
+export { assertNativeContextRecord, ACPX_BACKEND_ID, AcpRuntimeError, AcpxRuntime, DEFAULT_AGENT_NAME, REQUESTED_MODEL_UNSUPPORTED_ERROR_CODE, REQUESTED_MODEL_UNSUPPORTED_REASONS, RequestedModelUnsupportedError, createAcpRuntime, createAgentRegistry, createFileSessionStore, createRuntimeStore, decodeAcpxRuntimeHandleState, encodeAcpxRuntimeHandleState, isAcpRuntimeError, isRequestedModelUnsupportedError };
 
 //# sourceMappingURL=runtime.js.map
\ No newline at end of file
diff --git a/dist/runtime.d.ts b/dist/runtime.d.ts
index 0a8e55a..c7427da 100644
--- a/dist/runtime.d.ts
+++ b/dist/runtime.d.ts
@@ -1,4 +1,4 @@
-import { a as AcpPermissionRequest, c as NonInteractivePermissionPolicy, f as SessionRecord, i as AcpPermissionDecision, l as PermissionMode, n as SystemPromptOption, s as McpServer$1, t as SessionAgentOptions } from "./session-options-CTdZvr68.js";
+import { x as AcpRuntimeProcessEvent, a as AcpPermissionRequest, c as NonInteractivePermissionPolicy, f as SessionRecord, i as AcpPermissionDecision, l as PermissionMode, n as SystemPromptOption, s as McpServer$1, t as SessionAgentOptions } from "./session-options-CTdZvr68.js";
 import { a as RequestedModelUnsupportedErrorCode, i as RequestedModelUnsupportedError, n as REQUESTED_MODEL_UNSUPPORTED_ERROR_CODE, o as RequestedModelUnsupportedReason, r as REQUESTED_MODEL_UNSUPPORTED_REASONS, s as isRequestedModelUnsupportedError, t as AcpClient } from "./client-BeKvumq9.js";
 import fs from "node:fs";
 import { ToolCallContent, ToolCallLocation, ToolKind } from "@agentclientprotocol/sdk";
@@ -19,7 +19,26 @@ type AcpRuntimeHandle = {
   backendSessionId?: string;
   agentSessionId?: string;
 };
+export interface AcpRuntimeResourceScope {
+  begin(): AcpRuntimeResourceScope;
+  cleanup(): AcpRuntimeResourceScope;
+  beforeSpawn(): object;
+  spawned(permit: object, child: import('node:child_process').ChildProcess): void;
+  notCreated(permit: object): void;
+  finish(): void;
+}
+export interface AcpNativeContextExpected {
+  nativeCreationId: string; sessionKey: string; agent: string; command: string[]; cwd: string; executionDomain: string;
+}
+export interface AcpNativeContextIdentity extends AcpNativeContextExpected {
+  acpxRecordId: string; backendSessionId: string; agentSessionId?: string; defaults: {model?: string; reasoningEffort?: string};
+}
+export type AcpStrictCreateInput = AcpRuntimeEnsureInput & {creation: AcpNativeContextExpected; onPrepared(identity: AcpNativeContextIdentity, configuration: {model?:string;reasoningEffort?:string}): Promise<void> | void};
+export type AcpStrictRestoreInput = AcpRuntimeEnsureInput & {expected: AcpNativeContextIdentity; onPrepared(identity: AcpNativeContextIdentity, configuration: {model?:string;reasoningEffort?:string}): Promise<void> | void};
+export declare function assertNativeContextRecord(record: AcpSessionRecord, identity: AcpNativeContextIdentity): void;
+export interface AcpConfigurationAck { configId: string; value: string; configOptions: unknown[] }
 type AcpRuntimeEnsureInput = {
+  resourceScope?: AcpRuntimeResourceScope;
   sessionKey: string;
   agent: string;
   mode: AcpRuntimeSessionMode;
@@ -44,6 +63,8 @@ type AcpRuntimeTurnAttachment = {
   data: string;
 };
 type AcpRuntimeTurnInput = {
+  beforePrompt?: () => void;
+  resourceScope?: AcpRuntimeResourceScope;
   handle: AcpRuntimeHandle;
   text: string;
   attachments?: AcpRuntimeTurnAttachment[];
@@ -217,6 +238,9 @@ interface AcpRuntimeTurn {
 }
 interface AcpRuntime {
   ensureSession(input: AcpRuntimeEnsureInput): Promise<AcpRuntimeHandle>;
+  createStrictSession?(input: AcpStrictCreateInput): Promise<AcpRuntimeHandle>;
+  restoreStrictSession?(input: AcpStrictRestoreInput): Promise<AcpRuntimeHandle>;
+  setStrictConfigOption?(input: {handle: AcpRuntimeHandle; key: string; value: string}): Promise<AcpConfigurationAck>;
   startTurn(input: AcpRuntimeTurnInput): AcpRuntimeTurn;
   /**
    * Compatibility adapter for consumers that expect terminal status in the
@@ -261,6 +285,8 @@ interface AcpAgentRegistry {
   list(): string[];
 }
 type AcpRuntimeOptions = {
+  /** In-process ownership events; never persisted in session options. */
+  onProcess?: (event: AcpRuntimeProcessEvent) => void;
   cwd: string;
   sessionStore: AcpSessionStore;
   agentRegistry: AcpAgentRegistry;
@@ -416,6 +442,9 @@ declare class AcpxRuntime implements AcpxRuntimeLike {
   probeAvailability(): Promise<void>;
   doctor(): Promise<AcpRuntimeDoctorReport>;
   ensureSession(input: AcpRuntimeEnsureInput): Promise<AcpRuntimeHandle>;
+  createStrictSession(input: AcpStrictCreateInput): Promise<AcpRuntimeHandle>;
+  restoreStrictSession(input: AcpStrictRestoreInput): Promise<AcpRuntimeHandle>;
+  setStrictConfigOption(input: {handle: AcpRuntimeHandle; key: string; value: string}): Promise<AcpConfigurationAck>;
   startTurn(input: AcpRuntimeTurnInput): {
     requestId: string;
     events: {
@@ -465,5 +494,5 @@ declare function createRuntimeStore(options: {
   stateDir: string;
 }): AcpSessionStore;
 //#endregion
-export { ACPX_BACKEND_ID, type AcpAgentRegistry, type AcpFileSessionStoreOptions, type AcpPermissionDecision, type AcpPermissionRequest, type AcpRuntime, type AcpRuntimeAvailableCommand, type AcpRuntimeCapabilities, type AcpRuntimeDoctorReport, type AcpRuntimeEnsureInput, AcpRuntimeError, type AcpRuntimeErrorCode, type AcpRuntimeEvent, type AcpRuntimeHandle, type AcpRuntimeOptions, type AcpRuntimePromptMode, type AcpRuntimeSessionMode, type AcpRuntimeSessionModels, type AcpRuntimeSessionUsage, type AcpRuntimeStatus, type AcpRuntimeTurn, type AcpRuntimeTurnAttachment, type AcpRuntimeTurnInput, type AcpRuntimeTurnResult, type AcpRuntimeTurnResultError, type AcpRuntimeUsageBreakdown, type AcpRuntimeUsageCost, type AcpSessionRecord, type AcpSessionStore, type AcpSessionUpdateTag, AcpxRuntime, DEFAULT_AGENT_NAME, REQUESTED_MODEL_UNSUPPORTED_ERROR_CODE, REQUESTED_MODEL_UNSUPPORTED_REASONS, RequestedModelUnsupportedError, type RequestedModelUnsupportedErrorCode, type RequestedModelUnsupportedReason, type SessionAgentOptions, type SystemPromptOption, createAcpRuntime, createAgentRegistry, createFileSessionStore, createRuntimeStore, decodeAcpxRuntimeHandleState, encodeAcpxRuntimeHandleState, isAcpRuntimeError, isRequestedModelUnsupportedError };
+export { ACPX_BACKEND_ID, type AcpAgentRegistry, type AcpFileSessionStoreOptions, type AcpPermissionDecision, type AcpPermissionRequest, type AcpRuntime, type AcpRuntimeAvailableCommand, type AcpRuntimeCapabilities, type AcpRuntimeDoctorReport, type AcpRuntimeEnsureInput, AcpRuntimeError, type AcpRuntimeErrorCode, type AcpRuntimeEvent, type AcpRuntimeHandle, type AcpRuntimeOptions, type AcpRuntimeProcessEvent, type AcpRuntimePromptMode, type AcpRuntimeSessionMode, type AcpRuntimeSessionModels, type AcpRuntimeSessionUsage, type AcpRuntimeStatus, type AcpRuntimeTurn, type AcpRuntimeTurnAttachment, type AcpRuntimeTurnInput, type AcpRuntimeTurnResult, type AcpRuntimeTurnResultError, type AcpRuntimeUsageBreakdown, type AcpRuntimeUsageCost, type AcpSessionRecord, type AcpSessionStore, type AcpSessionUpdateTag, AcpxRuntime, DEFAULT_AGENT_NAME, REQUESTED_MODEL_UNSUPPORTED_ERROR_CODE, REQUESTED_MODEL_UNSUPPORTED_REASONS, RequestedModelUnsupportedError, type RequestedModelUnsupportedErrorCode, type RequestedModelUnsupportedReason, type SessionAgentOptions, type SystemPromptOption, createAcpRuntime, createAgentRegistry, createFileSessionStore, createRuntimeStore, decodeAcpxRuntimeHandleState, encodeAcpxRuntimeHandleState, isAcpRuntimeError, isRequestedModelUnsupportedError };
 //# sourceMappingURL=runtime.d.ts.map
\ No newline at end of file
diff --git a/dist/live-checkpoint-CBecfnSH.js b/dist/live-checkpoint-CBecfnSH.js
index 32fd9b0..18e8998 100644
--- a/dist/live-checkpoint-CBecfnSH.js
+++ b/dist/live-checkpoint-CBecfnSH.js
@@ -1356,6 +1356,8 @@ function parseAcpxState(raw) {
 	if (!record) return;
 	const state = {};
 	assignBooleanTrue(state, "reset_on_next_ensure", record.reset_on_next_ensure);
+	assignStringState(state, "dutydeck_native_creation_id", record.dutydeck_native_creation_id);
+	assignStringState(state, "dutydeck_native_identity", record.dutydeck_native_identity);
 	assignStringState(state, "current_mode_id", record.current_mode_id);
 	assignStringState(state, "desired_mode_id", record.desired_mode_id);
 	assignDesiredConfigOptions(state, record.desired_config_options);
@@ -2943,19 +2945,28 @@ function compareVersionParts(left, right) {
 	}
 	return 0;
 }
-async function detectGeminiVersion(command) {
-	const versionLine = (await readCommandOutput(command, ["--version"], GEMINI_VERSION_TIMEOUT_MS))?.split(/\r?\n/).map((line) => line.trim()).find((line) => /\d+\.\d+\.\d+/.test(line));
+function controlledResourceSpawn(scope, command, args, options) {
+  const permit = scope?.beforeSpawn();
+  let child;
+  try { child = spawn(command, args, options); }
+  catch (error) { scope?.notCreated(permit); throw error; }
+  try { scope?.spawned(permit, child); }
+  catch (error) { if (child.exitCode == null && child.signalCode == null) child.kill("SIGKILL"); throw error; }
+  return child;
+}
+async function detectGeminiVersion(command, onProcess, resourceScope) {
+	const versionLine = (await readCommandOutput(command, ["--version"], GEMINI_VERSION_TIMEOUT_MS, onProcess, resourceScope))?.split(/\r?\n/).map((line) => line.trim()).find((line) => /\d+\.\d+\.\d+/.test(line));
 	return parseGeminiVersion(versionLine);
 }
-async function resolveGeminiCommandArgs(command, args) {
+async function resolveGeminiCommandArgs(command, args, onProcess, resourceScope) {
 	if (basenameToken(command) !== "gemini" || !args.includes("--acp")) return [...args];
-	const version = await detectGeminiVersion(command);
+	const version = await detectGeminiVersion(command, onProcess, resourceScope);
 	if (version && compareVersionParts(version.parts, GEMINI_ACP_FLAG_VERSION) < 0) return args.map((arg) => arg === "--acp" ? "--experimental-acp" : arg);
 	return [...args];
 }
-async function readCommandOutput(command, args, timeoutMs) {
+async function readCommandOutput(command, args, timeoutMs, onProcess, resourceScope) {
 	return await new Promise((resolve) => {
-		const child = spawn(command, [...args], buildSpawnCommandOptions(command, {
+		const child = controlledResourceSpawn(resourceScope, command, [...args], buildSpawnCommandOptions(command, {
 			stdio: [
 				"ignore",
 				"pipe",
@@ -2963,6 +2974,7 @@ async function readCommandOutput(command, args, timeoutMs) {
 			],
 			windowsHide: true
 		}));
+		onProcess?.({ phase: "spawned", kind: "agent", child });
 		let stdout = "";
 		let stderr = "";
 		let settled = false;
@@ -2970,7 +2982,8 @@ async function readCommandOutput(command, args, timeoutMs) {
 			if (settled) return;
 			settled = true;
 			clearTimeout(timer);
-			child.removeAllListeners();
+			child.off("error", onError);
+			child.off("close", onClose);
 			child.stdout?.removeAllListeners();
 			child.stderr?.removeAllListeners();
 			resolve(value);
@@ -2987,17 +3000,15 @@ async function readCommandOutput(command, args, timeoutMs) {
 		child.stderr?.on("data", (chunk) => {
 			stderr += chunk;
 		});
-		child.once("error", () => {
-			finish(void 0);
-		});
-		child.once("close", () => {
-			finish(`${stdout}\n${stderr}`);
-		});
+		const onError = () => finish(void 0);
+		const onClose = () => finish(`${stdout}\n${stderr}`);
+		child.once("error", onError);
+		child.once("close", onClose);
 	});
 }
-async function buildGeminiAcpStartupTimeoutMessage(command) {
+async function buildGeminiAcpStartupTimeoutMessage(command, onProcess, resourceScope) {
 	const parts = ["Gemini CLI ACP startup timed out before initialize completed.", "This usually means the local Gemini CLI is waiting on interactive OAuth or has incompatible ACP subprocess behavior."];
-	const version = await detectGeminiVersion(command);
+	const version = await detectGeminiVersion(command, onProcess, resourceScope);
 	if (version) parts.push(`Detected Gemini CLI version: ${version.raw}.`);
 	if (!process.env.GEMINI_API_KEY && !process.env.GOOGLE_API_KEY) parts.push("No GEMINI_API_KEY or GOOGLE_API_KEY was set for non-interactive auth.");
 	parts.push("Try upgrading Gemini CLI and using API-key-based auth for non-interactive ACP runs.");
@@ -3010,16 +3021,16 @@ function buildClaudeAcpSessionCreateTimeoutMessage() {
 		"In harnessed or non-interactive runs, prefer --approve-all with nonInteractivePermissions=deny, upgrade Claude Code and the Claude ACP adapter, or use acpx claude exec as a one-shot fallback."
 	].join(" ");
 }
-async function buildCopilotAcpUnsupportedMessage(command) {
+async function buildCopilotAcpUnsupportedMessage(command, onProcess, resourceScope) {
 	const parts = ["GitHub Copilot CLI ACP stdio mode is not available in the installed copilot binary.", "acpx copilot expects a Copilot CLI release that supports --acp --stdio."];
-	const helpOutput = await readCommandOutput(command, ["--help"], COPILOT_HELP_TIMEOUT_MS);
+	const helpOutput = await readCommandOutput(command, ["--help"], COPILOT_HELP_TIMEOUT_MS, onProcess, resourceScope);
 	if (typeof helpOutput === "string" && !helpOutput.includes("--acp")) parts.push("Detected copilot --help output without --acp support.");
 	parts.push("Upgrade GitHub Copilot CLI to a release with ACP stdio support, or use --agent with another ACP-compatible adapter in the meantime.");
 	return parts.join(" ");
 }
-async function ensureCopilotAcpSupport(command) {
-	const helpOutput = await readCommandOutput(command, ["--help"], COPILOT_HELP_TIMEOUT_MS);
-	if (typeof helpOutput === "string" && !helpOutput.includes("--acp")) throw new CopilotAcpUnsupportedError(await buildCopilotAcpUnsupportedMessage(command), { retryable: false });
+async function ensureCopilotAcpSupport(command, onProcess, resourceScope) {
+	const helpOutput = await readCommandOutput(command, ["--help"], COPILOT_HELP_TIMEOUT_MS, onProcess, resourceScope);
+	if (typeof helpOutput === "string" && !helpOutput.includes("--acp")) throw new CopilotAcpUnsupportedError(await buildCopilotAcpUnsupportedMessage(command, onProcess, resourceScope), { retryable: false });
 }
 function buildClaudeCodeOptionsMeta(options, isolateUserSettings = false) {
 	const claudeCodeOptions = {};
@@ -3382,6 +3393,10 @@ var TerminalManager = class {
 		this.permissionMode = options.permissionMode;
 		this.nonInteractivePermissions = options.nonInteractivePermissions ?? "deny";
 		this.onOperation = options.onOperation;
+		this.onProcess = options.onProcess;
+		this.resourceScope = options.resourceScope;
+		this.creationGeneration = 0;
+		this.closed = false;
 		this.usesDefaultConfirmExecute = options.confirmExecute == null;
 		this.confirmExecute = options.confirmExecute ?? defaultConfirmExecute;
 		this.killGraceMs = Math.max(0, Math.round(options.killGraceMs ?? DEFAULT_KILL_GRACE_MS));
@@ -3390,7 +3405,12 @@ var TerminalManager = class {
 		this.permissionMode = permissionMode;
 		this.nonInteractivePermissions = nonInteractivePermissions ?? "deny";
 	}
-	async createTerminal(params) {
+	async createTerminal(params, parentScope) {
+		const resourceScope = parentScope?.begin();
+		const creation = {};
+		const generation = this.creationGeneration;
+		this.onProcess?.({ phase: "creation-started", kind: "terminal", creation });
+		try {
 		const commandLine = toCommandLine(params.command, params.args);
 		const summary = `terminal/create: ${commandLine}`;
 		this.emitOperation({
@@ -3401,14 +3421,20 @@ var TerminalManager = class {
 		});
 		try {
 			if (!await this.isExecuteApproved(commandLine)) throw new PermissionDeniedError("Permission denied for terminal/create");
+			if (this.closed || generation !== this.creationGeneration) throw new Error("ACP terminal owner closed");
 			const outputByteLimit = Math.max(0, Math.round(params.outputByteLimit ?? DEFAULT_TERMINAL_OUTPUT_LIMIT_BYTES));
-			const { proc, spawnCommand } = await spawnTerminalProcess(params, this.cwd);
+			const { proc, spawnCommand } = await spawnTerminalProcess(params, this.cwd, this.onProcess, resourceScope, () => { if (this.closed || generation !== this.creationGeneration) throw new Error("ACP terminal owner closed"); });
+			if (this.closed || generation !== this.creationGeneration) {
+				try { proc.kill("SIGKILL"); } catch {}
+				throw new Error("ACP terminal owner closed");
+			}
 			let resolveExit = () => {};
 			const exitPromise = new Promise((resolve) => {
 				resolveExit = resolve;
 			});
 			const terminal = {
 				process: proc,
+				resourceScope,
 				killProcessGroup: spawnCommand.killProcessGroup,
 				descendantPids: /* @__PURE__ */ new Set(),
 				output: Buffer.alloc(0),
@@ -3463,6 +3489,7 @@ var TerminalManager = class {
 			});
 			throw error;
 		}
+		} finally { try { resourceScope?.finish(); } finally { this.onProcess?.({ phase: "creation-finished", kind: "terminal", creation }); } }
 	}
 	async terminalOutput(params) {
 		const terminal = this.getTerminal(params.terminalId);
@@ -3571,6 +3598,8 @@ var TerminalManager = class {
 		}
 	}
 	async shutdown() {
+		this.closed = true;
+		this.creationGeneration++;
 		for (const terminalId of Array.from(this.terminals.keys())) await this.releaseTerminal({
 			terminalId,
 			sessionId: "shutdown"
@@ -3607,36 +3636,39 @@ var TerminalManager = class {
 		await this.waitForCleanupAfterSignal(terminal);
 	}
 	async signalProcess(terminal, signal) {
+		const scope = this.resourceScope?.cleanup();
+		try {
 		const pid = terminal.process.pid;
 		if (terminal.killProcessGroup && pid && process.platform === "win32") {
-			await this.signalWindowsProcessGroup(terminal, pid, signal);
+			await this.signalWindowsProcessGroup(terminal, pid, signal, scope);
 			return;
 		}
 		if (terminal.killProcessGroup && pid) {
-			await this.signalPosixProcessGroup(terminal, pid, signal);
+			await this.signalPosixProcessGroup(terminal, pid, signal, scope);
 			return;
 		}
 		terminal.process.kill(signal);
+		} finally { scope?.finish(); }
 	}
-	async signalWindowsProcessGroup(terminal, pid, signal) {
-		await this.captureDescendantPids(terminal, pid);
+	async signalWindowsProcessGroup(terminal, pid, signal, scope) {
+		await this.captureDescendantPids(terminal, pid, scope);
 		if (this.isRunning(terminal)) {
-			await killWindowsProcessTree(pid, signal);
+			await killWindowsProcessTree(pid, signal, scope);
 			return;
 		}
-		for (const descendantPid of terminal.descendantPids) await killWindowsProcessTree(descendantPid, signal);
+		for (const descendantPid of terminal.descendantPids) await killWindowsProcessTree(descendantPid, signal, scope);
 	}
-	async signalPosixProcessGroup(terminal, pid, signal) {
-		await this.captureDescendantPids(terminal, pid);
+	async signalPosixProcessGroup(terminal, pid, signal, scope) {
+		await this.captureDescendantPids(terminal, pid, scope);
 		if (hasLiveProcessGroup(pid)) {
 			sendSignal(-pid, signal);
 			return;
 		}
 		for (const descendantPid of terminal.descendantPids) sendSignal(descendantPid, signal);
 	}
-	async captureDescendantPids(terminal, pid) {
+	async captureDescendantPids(terminal, pid, scope) {
 		if (!this.isRunning(terminal)) await terminal.processGroupSnapshotPromise?.catch(() => {});
-		for (const descendantPid of await listDescendantPids(pid)) terminal.descendantPids.add(descendantPid);
+		for (const descendantPid of await listDescendantPids(pid, scope)) terminal.descendantPids.add(descendantPid);
 	}
 	async waitForCleanupAfterSignal(terminal) {
 		return await Promise.race([this.waitForTerminalAndTrackedDescendants(terminal).then(() => true), waitMs(this.killGraceMs).then(() => false)]);
@@ -3647,26 +3679,28 @@ var TerminalManager = class {
 		while (hasLivePid(terminal.descendantPids)) await waitMs(25);
 	}
 };
-async function spawnTerminalProcess(params, defaultCwd) {
+async function spawnTerminalProcess(params, defaultCwd, onProcess, resourceScope, assertOwner) {
 	const directCommand = buildTerminalSpawnCommand(params.command, params.args);
 	try {
 		return {
-			proc: await spawnAndWait(directCommand, params, defaultCwd),
+			proc: await spawnAndWait(directCommand, params, defaultCwd, onProcess, resourceScope, assertOwner),
 			spawnCommand: directCommand
 		};
 	} catch (error) {
 		const fallbackCommand = params.args === void 0 && isNotFoundSpawnError(error) ? buildTerminalFallbackSpawnCommand(params.command, params.cwd ?? defaultCwd) : void 0;
 		if (!fallbackCommand) throw error;
 		return {
-			proc: await spawnAndWait(fallbackCommand, params, defaultCwd),
+			proc: await spawnAndWait(fallbackCommand, params, defaultCwd, onProcess, resourceScope, assertOwner),
 			spawnCommand: fallbackCommand
 		};
 	}
 }
-async function spawnAndWait(spawnCommand, params, defaultCwd) {
+async function spawnAndWait(spawnCommand, params, defaultCwd, onProcess, resourceScope, assertOwner) {
 	const spawnOptions = buildTerminalSpawnOptions(spawnCommand.command, params.cwd ?? defaultCwd, params.env);
 	if (spawnCommand.killProcessGroup) spawnOptions.detached = true;
-	const proc = spawn(spawnCommand.command, spawnCommand.args, spawnOptions);
+	assertOwner?.();
+	const proc = controlledResourceSpawn(resourceScope, spawnCommand.command, spawnCommand.args, spawnOptions);
+	onProcess?.({ phase: "spawned", kind: "terminal", child: proc });
 	await waitForSpawn(proc);
 	return proc;
 }
@@ -3689,10 +3723,10 @@ function commandPathExists(command, cwd) {
 	const resolvedPath = path.isAbsolute(command) ? command : path.resolve(cwd, command);
 	return fs.existsSync(resolvedPath);
 }
-async function listDescendantPids(rootPid) {
+async function listDescendantPids(rootPid, scope) {
 	let output;
 	try {
-		output = await runProcessListCommand();
+		output = await runProcessListCommand(scope);
 	} catch {
 		return [];
 	}
@@ -3725,10 +3759,10 @@ function parseProcessListLine(line) {
 		parentPid
 	};
 }
-async function runProcessListCommand() {
-	if (process.platform === "win32") return await runWindowsProcessListCommand();
+async function runProcessListCommand(scope) {
+	if (process.platform === "win32") return await runWindowsProcessListCommand(scope);
 	return await new Promise((resolve, reject) => {
-		const child = spawn("ps", ["-eo", "pid=,ppid="], { stdio: [
+		const child = controlledResourceSpawn(scope, "ps", ["-eo", "pid=,ppid="], { stdio: [
 			"ignore",
 			"pipe",
 			"pipe"
@@ -3754,18 +3788,21 @@ async function runProcessListCommand() {
 	});
 }
 async function rememberProcessGroupPids(terminal) {
+	const scope = terminal.resourceScope?.cleanup();
+	try {
 	const processGroupId = terminal.process.pid;
 	if (!terminal.killProcessGroup || !processGroupId) return;
 	if (process.platform === "win32") {
-		for (const pid of await listDescendantPids(processGroupId)) terminal.descendantPids.add(pid);
+		for (const pid of await listDescendantPids(processGroupId, scope)) terminal.descendantPids.add(pid);
 		return;
 	}
-	for (const pid of await listProcessGroupPids(processGroupId)) if (pid !== processGroupId) terminal.descendantPids.add(pid);
+	for (const pid of await listProcessGroupPids(processGroupId, scope)) if (pid !== processGroupId) terminal.descendantPids.add(pid);
+	} finally {scope?.finish();}
 }
-async function listProcessGroupPids(processGroupId) {
+async function listProcessGroupPids(processGroupId, scope) {
 	let output;
 	try {
-		output = await runProcessGroupListCommand();
+		output = await runProcessGroupListCommand(scope);
 	} catch {
 		return [];
 	}
@@ -3779,9 +3816,9 @@ async function listProcessGroupPids(processGroupId) {
 	}
 	return pids;
 }
-async function runProcessGroupListCommand() {
+async function runProcessGroupListCommand(scope) {
 	return await new Promise((resolve, reject) => {
-		const child = spawn("ps", ["-eo", "pid=,pgid="], { stdio: [
+		const child = controlledResourceSpawn(scope, "ps", ["-eo", "pid=,pgid="], { stdio: [
 			"ignore",
 			"pipe",
 			"pipe"
@@ -3806,9 +3843,9 @@ async function runProcessGroupListCommand() {
 		});
 	});
 }
-async function runWindowsProcessListCommand() {
+async function runWindowsProcessListCommand(scope) {
 	return await new Promise((resolve, reject) => {
-		const child = spawn("powershell.exe", [
+		const child = controlledResourceSpawn(scope, "powershell.exe", [
 			"-NoProfile",
 			"-NonInteractive",
 			"-Command",
@@ -3841,7 +3878,7 @@ async function runWindowsProcessListCommand() {
 		});
 	});
 }
-async function killWindowsProcessTree(pid, signal) {
+async function killWindowsProcessTree(pid, signal, scope) {
 	const args = [
 		"/pid",
 		String(pid),
@@ -3849,7 +3886,7 @@ async function killWindowsProcessTree(pid, signal) {
 	];
 	if (signal === "SIGKILL") args.push("/f");
 	await new Promise((resolve) => {
-		const child = spawn("taskkill", args, {
+		const child = controlledResourceSpawn(scope, "taskkill", args, {
 			stdio: [
 				"ignore",
 				"ignore",
@@ -4040,6 +4077,7 @@ var AcpClient = class {
 	cancellingSessionIds = /* @__PURE__ */ new Set();
 	permissionAbortControllers = /* @__PURE__ */ new Map();
 	closing = false;
+	creationGeneration = 0;
 	agentStartedAt;
 	lastAgentExit;
 	lastKnownPid;
@@ -4069,6 +4107,8 @@ var AcpClient = class {
 			}
 		});
 		this.terminalManager = new TerminalManager({
+			onProcess: this.options.onProcess,
+			resourceScope: this.options.resourceScope,
 			cwd: this.options.cwd,
 			permissionMode: this.options.permissionMode,
 			nonInteractivePermissions: this.options.nonInteractivePermissions,
@@ -4142,12 +4182,23 @@ var AcpClient = class {
 		return this.activePrompt.sessionId === sessionId;
 	}
 	async start() {
+		const resourceScope = this.options.resourceScope?.begin();
+		const creation = {};
+		this.options.onProcess?.({ phase: "creation-started", kind: "agent", creation });
+		try {
 		if (this.connection && this.agent && isChildProcessRunning(this.agent)) return;
+		const generation = this.creationGeneration + (this.connection || this.agent ? 1 : 0);
 		if (this.connection || this.agent) await this.close();
-		const launch = await this.resolveAgentLaunchPlan();
+		const assertOwner = () => { if (generation !== this.creationGeneration) throw new Error("ACP client closed during startup"); };
+		assertOwner();
+		this.terminalManager.closed = false;
+		const launch = await this.resolveAgentLaunchPlan(resourceScope);
+		assertOwner();
 		this.logAgentLaunch(launch);
-		await this.ensureLaunchSupport(launch);
-		const child = await this.spawnAgentProcess(launch);
+		await this.ensureLaunchSupport(launch, resourceScope);
+		assertOwner();
+		const child = await this.spawnAgentProcess(launch, resourceScope);
+		if (generation !== this.creationGeneration) { await this.terminateAgentProcess(child); assertOwner(); }
 		this.closing = false;
 		this.agentStartedAt = isoNow$1();
 		this.lastAgentExit = void 0;
@@ -4168,19 +4219,21 @@ var AcpClient = class {
 		}, { once: true });
 		const startupFailure = this.createStartupFailureWatcher(child, startupStderr);
 		await this.initializeAgentConnection({
+			generation,
 			child,
 			connection,
 			startupFailure,
 			startupStderr,
-			launch
+			launch, resourceScope
 		});
+		} finally { try { resourceScope?.finish(); } finally { this.options.onProcess?.({ phase: "creation-finished", kind: "agent", creation }); } }
 	}
-	async resolveAgentLaunchPlan() {
+	async resolveAgentLaunchPlan(resourceScope) {
 		const configuredCommand = resolveAgentCommandParts(this.options.agentCommand, this.options.agentArgv);
 		const resolvedBuiltInLaunch = resolveBuiltInAgentLaunch(this.options.agentCommand);
 		const spawnCommand = resolvedBuiltInLaunch?.command ?? configuredCommand.command;
 		let args = resolvedBuiltInLaunch?.args ?? configuredCommand.args;
-		args = await resolveGeminiCommandArgs(spawnCommand, args);
+		args = await resolveGeminiCommandArgs(spawnCommand, args, this.options.onProcess, resourceScope);
 		if (isQoderAcpCommand(spawnCommand, args)) args = buildQoderAcpCommandArgs(args, this.options);
 		return {
 			spawnCommand,
@@ -4205,8 +4258,8 @@ var AcpClient = class {
 		}
 		this.log(`spawning agent: ${plan.spawnCommand} ${plan.args.join(" ")}`);
 	}
-	async ensureLaunchSupport(plan) {
-		if (plan.copilotAcp) await ensureCopilotAcpSupport(plan.spawnCommand);
+	async ensureLaunchSupport(plan, resourceScope) {
+		if (plan.copilotAcp) await ensureCopilotAcpSupport(plan.spawnCommand, this.options.onProcess, resourceScope);
 		if (!plan.claudeAcp) return;
 		const claudeExe = resolveClaudeCodeExecutable(process.platform, plan.spawnOptions.env);
 		if (claudeExe) {
@@ -4214,12 +4267,13 @@ var AcpClient = class {
 			this.log(`resolved system Claude Code executable: ${claudeExe}`);
 		}
 	}
-	async spawnAgentProcess(plan) {
+	async spawnAgentProcess(plan, resourceScope) {
 		const spawnCommand = buildAgentSpawnCommand(plan.spawnCommand, plan.args, process.platform, plan.spawnOptions.env);
-		const spawnedChild = spawn(spawnCommand.command, spawnCommand.args, {
+		const spawnedChild = controlledResourceSpawn(resourceScope, spawnCommand.command, spawnCommand.args, {
 			...plan.spawnOptions,
 			windowsVerbatimArguments: spawnCommand.windowsVerbatimArguments
 		});
+		this.options.onProcess?.({ phase: "spawned", kind: "agent", child: spawnedChild });
 		try {
 			await waitForSpawn$1(spawnedChild);
 		} catch (error) {
@@ -4269,6 +4323,10 @@ var AcpClient = class {
 		try {
 			const initResult = await Promise.race([this.initializeProtocolConnection(params.connection, params.launch), params.startupFailure.promise]);
 			params.startupFailure.dispose();
+			if (params.generation !== this.creationGeneration) {
+				await this.terminateAgentProcess(params.child);
+				throw new Error("ACP client closed during startup");
+			}
 			this.connection = params.connection;
 			this.agent = params.child;
 			this.initResult = initResult;
@@ -4278,13 +4336,13 @@ var AcpClient = class {
 		}
 	}
 	async initializeProtocolConnection(connection, launch) {
+		const capabilities = resolveClientCapabilities({ devinAcp: launch.devinAcp, fs: this.options.fs !== false, terminal: this.options.terminal !== false });
 		const initializePromise = connection.initialize({
 			protocolVersion: PROTOCOL_VERSION,
-			clientCapabilities: resolveClientCapabilities({
-				devinAcp: launch.devinAcp,
-				fs: this.options.fs !== false,
-				terminal: this.options.terminal !== false
-			}),
+			clientCapabilities: {
+				...capabilities,
+				_meta: { ...capabilities._meta, jetbrains: { air: { version: 1, capabilities: ["sessionFailure"] } } }
+			},
 			clientInfo: resolveClientInfo(launch.devinAcp)
 		});
 		const initialized = launch.geminiAcp ? await withTimeout(initializePromise, resolveGeminiAcpStartupTimeoutMs()) : await initializePromise;
@@ -4297,7 +4355,7 @@ var AcpClient = class {
 		try {
 			params.child.kill();
 		} catch {}
-		if (params.launch.geminiAcp && error instanceof TimeoutError) throw new GeminiAcpStartupTimeoutError(await buildGeminiAcpStartupTimeoutMessage(params.launch.spawnCommand), {
+		if (params.launch.geminiAcp && error instanceof TimeoutError) throw new GeminiAcpStartupTimeoutError(await buildGeminiAcpStartupTimeoutMessage(params.launch.spawnCommand, this.options.onProcess, params.resourceScope), {
 			cause: error,
 			retryable: true
 		});
@@ -4426,7 +4484,7 @@ var AcpClient = class {
 		this.suppressSessionUpdates = previous.suppressSessionUpdates;
 		this.suppressReplaySessionUpdateMessages = previous.suppressReplaySessionUpdateMessages;
 	}
-	async prompt(sessionId, prompt) {
+	async prompt(sessionId, prompt, resourceScope) {
 		const connection = this.getConnection();
 		const normalizedPrompt = this.normalizePromptForAgent(prompt);
 		const restoreConsoleError = this.options.suppressSdkConsoleErrors ? installSdkConsoleErrorSuppression() : void 0;
@@ -4441,6 +4499,7 @@ var AcpClient = class {
 			throw error;
 		}
 		this.activePrompt = {
+			resourceScope,
 			sessionId,
 			promise: promptPromise
 		};
@@ -4611,6 +4670,7 @@ var AcpClient = class {
 		}
 	}
 	async close() {
+		this.creationGeneration++;
 		this.closing = true;
 		await this.terminalManager.shutdown();
 		const agent = this.agent;
@@ -4943,7 +5003,9 @@ var AcpClient = class {
 	}
 	async handleCreateTerminal(params) {
 		try {
-			return await this.terminalManager.createTerminal(params);
+			const scope = this.activePrompt?.sessionId === params.sessionId ? this.activePrompt.resourceScope : void 0;
+			if (this.options.resourceScope && !scope) throw new Error("ACP_TERMINAL_SUBMISSION_SCOPE_REQUIRED");
+			return await this.terminalManager.createTerminal(params, scope);
 		} catch (error) {
 			this.recordPermissionError(params.sessionId, error);
 			throw error;
@@ -5540,6 +5602,8 @@ function cloneUsageCost(cost) {
 function cloneSessionAcpxState(state) {
 	if (!state) return;
 	return {
+		dutydeck_native_creation_id: state.dutydeck_native_creation_id,
+		dutydeck_native_identity: state.dutydeck_native_identity,
 		current_mode_id: state.current_mode_id,
 		desired_mode_id: state.desired_mode_id,
 		desired_config_options: state.desired_config_options ? { ...state.desired_config_options } : void 0,
@@ -6278,7 +6342,8 @@ const SESSION_REPLY_IDLE_MS = 1e3;
 const SESSION_REPLY_DRAIN_TIMEOUT_MS = 5e3;
 async function runPromptTurn(params) {
 	try {
-		const promptPromise = params.client.prompt(params.sessionId, params.prompt);
+		params.beforePrompt?.();
+		const promptPromise = params.client.prompt(params.sessionId, params.prompt, params.resourceScope);
 		await params.onPromptStarted?.();
 		const response = await withTimeout(promptPromise, params.timeoutMs);
 		await params.client.waitForSessionUpdatesIdle?.({
@@ -6288,6 +6353,7 @@ async function runPromptTurn(params) {
 		recordPromptResponseUsage(params.conversation, response.usage, params.promptMessageId);
 		return {
 			stopReason: response.stopReason,
+			_meta: response._meta,
 			source: "rpc"
 		};
 	} catch (error) {
diff --git a/dist/session-options-CTdZvr68.d.ts b/dist/session-options-CTdZvr68.d.ts
index 3ec271c..67062f8 100644
--- a/dist/session-options-CTdZvr68.d.ts
+++ b/dist/session-options-CTdZvr68.d.ts
@@ -1,3 +1,4 @@
+import type { ChildProcess } from 'node:child_process';
 import { AgentCapabilities, AnyMessage, ContentBlock, McpServer, McpServer as McpServer$1, RequestPermissionRequest, SessionConfigOption, SessionNotification, SetSessionConfigOptionResponse, ToolKind } from "@agentclientprotocol/sdk";
 //#region src/prompt-content.d.ts
 type PromptInput = ContentBlock[];
@@ -71,7 +72,9 @@ type SessionEventLog = {
   last_write_at?: string;
   last_write_error?: string | null;
 };
+type AcpProcessEvent = { phase: 'creation-started'; kind: 'agent' | 'terminal'; creation: object } | { phase: 'creation-finished'; kind: 'agent' | 'terminal'; creation: object } | { phase: 'spawned'; kind: 'agent' | 'terminal'; child: ChildProcess };
 type AcpClientOptions = {
+  onProcess?: (event: AcpProcessEvent) => void;
   agentCommand: string;
   agentArgv?: string[];
   cwd: string;
@@ -191,6 +194,8 @@ type SessionAvailableCommand = {
   has_input?: boolean;
 };
 type SessionAcpxState = {
+  dutydeck_native_creation_id?: string;
+  dutydeck_native_identity?: string;
   reset_on_next_ensure?: boolean;
   current_mode_id?: string;
   desired_mode_id?: string;
@@ -272,5 +277,5 @@ type SessionAgentOptions = {
   env?: Record<string, string>;
 };
 //#endregion
-export { AcpPermissionRequest as a, NonInteractivePermissionPolicy as c, PermissionStats as d, SessionRecord as f, AcpPermissionDecision as i, PermissionMode as l, SystemPromptOption as n, AuthPolicy as o, PromptInput as p, AcpClientOptions as r, McpServer$1 as s, SessionAgentOptions as t, PermissionPolicy as u };
+export { AcpProcessEvent as x, AcpPermissionRequest as a, NonInteractivePermissionPolicy as c, PermissionStats as d, SessionRecord as f, AcpPermissionDecision as i, PermissionMode as l, SystemPromptOption as n, AuthPolicy as o, PromptInput as p, AcpClientOptions as r, McpServer$1 as s, SessionAgentOptions as t, PermissionPolicy as u };
 //# sourceMappingURL=session-options-CTdZvr68.d.ts.map
\ No newline at end of file
