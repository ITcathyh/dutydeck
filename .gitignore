node_modules/
dist/
apps/web/dist/
apps/server/dist/
packages/*/dist/
.env
.env.local
*.log
.DS_Store
.dutydeck/
# 升级期仍会存在：迁移前、或迁移因目标已存在而跳过时，旧状态目录还留在原地。
.dockmux/
apps/server/public
*.tsbuildinfo

# setup 写入的 .env 备份（含本机路径，权限 600）
.env.bak.*

# 端到端验收产物
artifacts/

# 改版对比截图：4.9M 二进制，一轮改版就作废一批，进 git 会永久留在历史里。
# 需要时用 scripts/shoot-redesign.mjs 重跑即可（脚本本身是提交的）。
docs/assets/redesign-*/

# 卡片改版对比预览：改一轮就作废一批，需要时用 scripts/preview-lark-card.mts 重跑。
docs/assets/card-preview*/
